# Codex Pets 功能复刻进度

目标：用我们的 Clawd 桌宠复刻 OpenAI **Codex Pets** 的桌面伴随功能（保留我们自己的形象与"只读监控"定位）。
清单来自多代理调研（官方文档 + 开源克隆 `clawd-on-desk` 源码 + 桌宠 UX 研究）。

## ✅ 已具备（基线 / 本轮已补）
- [x] 常驻置顶透明浮窗、右下角锚定、可拖动
- [x] 实时状态气泡（主动显示"在做什么 / 需要你"）
- [x] 点桌宠/气泡 → 展开面板（任务 + token 用量 + 5h/7d 配额）
- [x] 红钟"等你确认"语义（WAIT 态）
- [x] 系统通知（**默认关**，避免与 Claude 桌面端重复；托盘可开）
- [x] 托盘菜单（显隐 / 窗口范围 / 刷新率 / 实时用量 / 通知开关）
- [x] **多任务聚合**（领先 Codex：Codex 只盯一个线程，我们跨全部会话取最高优先级）
- [x] **P0 持续呼吸**：任何情绪都轻微起伏（`#breath`，脚不动），工作/思考时呼吸加快
- [x] **P0 随机眨眼**：除睡着外所有情绪，间隔随机 + 偶尔双眨
- [x] **P0 阶段动词气泡**：思考中 / 运行命令 / 写改动 / 读取检索 / 跑测试 / 联网浏览 / 整理计划（替代裸工具名，剥 mcp 前缀）
- [x] **P0 ✅完成态**：一轮干净结束→气泡显示"✅ 完成" + 庆祝跳动（不再只靠通知）
- [x] **P0 气泡持久化策略**：需要你/出错/限流 **常驻不消失**；工作/思考/完成 **停留后淡出**、内容变化才重弹；带弹入/淡出动画
- [x] **P0 富情绪**：idle/thinking/working/done/needs/error（+ 预留 sleeping）各自光晕
- [x] **P0 转场一次性反应**：完成=庆祝跳、转入需要你/出错=晃动（基于真·状态跃迁，首帧不误触发）
- [x] **P0 尊重 `prefers-reduced-motion`**（关闭呼吸 + 跳过 JS 动画）

## ✅ P1（已完成）
- [x] **视线跟随光标**：main 每 150ms `getCursorScreenPoint` → IPC → 平移 `#eyes`（dist/300 归一、夹 0.85/0.5；睡着/动效关 不跟）
- [x] **空闲→睡眠阶梯**：按**用户**鼠标不活动 ~60s 且任务空闲/完成 → 睡着（眼眯成线、慢呼吸、暗光晕）；鼠标一动惊醒小跳。睡眠看用户不活动、情绪看任务，两者并存
- [x] **随机小动作**：30–60s 随机看一眼/歪头（仅醒着且空闲时）
- [x] **动效总开关**：托盘"动效"复选框（运行时 IPC `set-motion`）+ `prefers-reduced-motion`
- [x] **未处理再提醒**：需要你/出错气泡未消失时每 45s 温和晃动

## ✅ 高级化 / 实用性（2026-06-16 本轮）
- [x] **实时限额默认开**：`liveUsage` 默认 true + 启动即 `startUsage()`；无 `~/.claude/.credentials.json` 时走**钥匙串多 service 回退**（`Claude Code-credentials` 等，首次弹系统授权）。展开面板 `#quota` 显示 5h/7d/Sonnet/Opus/Cowork 利用率 + 重置倒计时 + 额外用量，与 Claude 桌面端 "Plan usage" 一致
- [x] **气泡加宽不截断**：`max-width 300px` + 2 行 `-webkit-line-clamp`（原来单行省略号，又小又窄已修）
- [x] **emoji → 精致 SVG 图标**：done/wait/error/rate/working(转圈)/thinking(慢转)/folder，`stroke=currentColor` 单色、按气泡类着色（等待琥珀/出错红/运行·完成绿/思考蓝），`@keyframes spin360`，动效关时停转
- [x] **配额条蓝色化**：基线 `#5b8cf0`（≥70% 琥珀、≥90% 红），对齐截图蓝色进度条
- [x] **面板细节高级化**：标题 🐾 → Claude 橙圆角点 `.title-dot`、目录链接 → 24px 描边图标按钮 `.openbtn`
- [x] **Clawd 可调大小**：`--pet` CSS 变量 + main `petPx`/`SZ(mode)` 窗口随尺寸缩放 + 托盘"桌宠大小"（小100/中120/大156）+ IPC `set-pet`；视线锚点随 petPx 缩放

## ✅ 多任务实时状态行栈（2026-06-16）
- [x] 折叠态从"单条 headline 气泡"改为**实时状态行栈**：每个进行中/待关注会话一行（最多 5 行 + "还有 N 个"），keyed reconcile 原地增删/更新、**绝不反复重弹**（根治用户反馈的"状态消息不停跳"），窗口高度随行数增长（渲染端量高 → `resize-bubble` 按右下角锚点调窗）。
- [x] 每行显示**当前活动摘要**（对标 Claude Code 状态行）：collector `toolActivity()` 从 transcript 末个工具调用抽人话——Bash→`description`、Read→文件名、Edit→文件名、Grep→pattern、WebSearch→query、Agent→description 等；运行行随活动原地更新文案。
- [x] 排序：出错/限流/等你 置顶，运行其次，完成垫底；完成态短驻 6.5s 后离栈、配额告急行短驻 8s（红光晕常驻）。

## ⬜ 待补（P2，锦上添花）
- [ ] 多击逗弄/眩晕彩蛋（400ms 累计；双击=戳一下，四连击=甩头）
- [ ] 拖拽弹簧/挤压物理 + pointerCapture + 记住位置
- [ ] 边缘吸附"迷你模式"（半身藏边，hover 探头）
- [ ] 可选音效（10s 冷却 + 睡眠/勿扰静音，**默认关**）
- [ ] Claude Code **hook 事件**驱动阶段（替代轮询，状态实时精确）
- [ ] 全局召唤/隐藏快捷键

## 🚫 明确非目标（Codex/clawd 有，但本项目不做）
- 交互式 允许/拒绝 审批气泡（交互已于 2026-06-15 推迟；核心是只读监控）
- 换皮 / 8 内置宠 / `/hatch` 自定义 / 人格模式（形象固定为官方 Clawd）
- 移动端 LAN 镜像 / Telegram 远程批准

> 完整调研原文：见会话工作流 `wu0et3216` 输出。
